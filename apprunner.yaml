version: 1.0

runtime:
  name: python
  version: 3.0 # Supported Python versions are 3.7, 3.8, and 3.9

build:
  commands:
    # No build commands needed for Python

routes:
  web:
    healthcheck-path: / 
    healthcheck-interval: 5
    healthcheck-timeout: 2
    environment-variables:
      PORT: 8080

# Specify the directory containing your Python application
artifacts:
  location: /
